% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudotime.R
\name{pseudotime}
\alias{pseudotime}
\title{Compute pseudotime (expected hitting time/distance) on a KNN graph}
\usage{
pseudotime(
  data = NULL,
  root,
  terminal_cells = NULL,
  terminal_values = NULL,
  weighted = TRUE,
  K = 30,
  kernel = "SEMer",
  symmetrize = "max",
  graph = NULL,
  weights = NULL,
  method = "cg",
  nb_it = 1500,
  eps = 1e-15,
  iguess = NULL
)
}
\arguments{
\item{data}{numeric matrix (cells x features); required unless \code{graph} is supplied.
Used both to build the KNN graph (unless \code{graph} is given) and to resolve character
\code{root}/\code{terminal_cells} against \code{rownames(data)}.}

\item{root}{integer index or character rowname of the root (origin) cell.}

\item{terminal_cells}{integer indices or character rownames of terminal cells (optional).
Pseudotime at these cells is pinned to \code{terminal_values} rather than solved for.}

\item{terminal_values}{numeric; pseudotime value(s) to pin \code{terminal_cells} to. Defaults
to \code{1e9} for every terminal cell if not supplied.}

\item{weighted}{logical (default TRUE); expected hitting distance (edge-weighted) if TRUE,
expected hitting time (step count) if FALSE.}

\item{K}{integer (default 30); number of nearest neighbors.}

\item{kernel}{character (default "SEMer"); similarity kernel, see \code{\link{knn.adj2spadjsim}}.}

\item{symmetrize}{character (default "max"); how to symmetrize the KNN graph - one of
"max", "mean", "prob", "min", "none". See \code{\link{knn.spadj2sym}} and related.}

\item{graph}{similarity \code{sparseMatrix} (optional); a precomputed graph to use instead of
building one from \code{data} - e.g. to reuse the same graph across multiple roots. When
supplied, \code{K}/\code{kernel}/\code{symmetrize} are ignored.}

\item{weights}{matrix of edge weights (optional), used only when \code{graph} is supplied and
\code{weighted=TRUE}; see \code{\link{assign_distance}}.}

\item{method}{character (default "cg"); numerical solver, "cg" or "minres".}

\item{nb_it}{integer (default 1500); maximum solver iterations.}

\item{eps}{double (default 1e-15); solver error tolerance.}

\item{iguess}{numeric vector (optional); initial guess for the iterative solver.}
}
\value{
a numeric vector of pseudotime values, one per row of \code{data} (or per vertex of
\code{graph}), named by \code{rownames(data)} if present. Solver diagnostics (\code{nb_it},
\code{error}) are attached as \code{attr(result, "diagnostics")}.
}
\description{
\code{pseudotime} computes the expected hitting time (\code{weighted=FALSE}) or expected
hitting distance (\code{weighted=TRUE}) from a root cell to every other cell, over a
K-nearest-neighbor graph - the same graph-Laplacian boundary-value solver used internally
by \code{\link{Pseudotime.tviblindi}}, exposed here as a standalone function that only needs
a plain data matrix (or a precomputed graph), not a \code{tviblindi} object. Optionally, one
or more terminal cells can be pinned to a fixed (typically large) pseudotime value - mirroring
how e.g. Palantir's \code{early_cell}/\code{terminal_states} work - to mark known endpoints.
}
