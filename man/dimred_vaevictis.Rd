% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimred_vaevictis.R
\name{dimred_vaevictis}
\alias{dimred_vaevictis}
\title{Dimensionality reduction via vaevictis (deep autoencoder)}
\usage{
dimred_vaevictis(
  x,
  data,
  dim = 2,
  vsplit = 0.1,
  enc_shape = c(128, 128, 128),
  dec_shape = c(128, 128, 128),
  perplexity = 10,
  batch_size = 512L,
  epochs = 100L,
  patience = 1L,
  ivis_pretrain = 0,
  ww = c(10, 10, 1, 1),
  margin = 1,
  shuffle = FALSE,
  K = 30,
  load_model = NULL,
  upsample = list(N = 5000, cluster = 15, method = "kmeans"),
  labels_name = names(x$labels)[1],
  ...
)
}
\arguments{
\item{x}{tviblindi class object; may be mutated (\code{x$vae} is set to the trained/loaded model).}

\item{data}{numeric matrix; the resolved input data (raw or denoised), as passed by \code{DimRed}.}

\item{dim}{integer (default 2); dimension of reduced data.}

\item{vsplit}{double (default 0.1); percentage of data used as validation step.}

\item{enc_shape}{integer vector (default \code{c(128,128,128)}); shape (depth and width) of the encoder.}

\item{dec_shape}{integer vector (default \code{c(128,128,128)}); shape (depth and width) of decoder.}

\item{perplexity}{double (default 10.); perplexity for tsne regularisation, see https://www.nature.com/articles/s41467-018-04368-5.}

\item{batch_size}{integer (default 512); training batch size.}

\item{epochs}{integer (default 100); maximum number of training epochs.}

\item{patience}{integer (default 1); early-stopping patience.}

\item{ivis_pretrain}{integer (default 0); ivis pretraining epochs.}

\item{ww}{numeric vector (default \code{c(10,10,1,1)}); loss weights - tsne regularisation, ivis pn loss, reconstruction error, KL divergence.}

\item{margin}{double (default 1); ivis pn loss margin.}

\item{shuffle}{logical (default FALSE); shuffle data before validation split; involves recomputation of the KNN matrix.}

\item{K}{integer (default 30); number of nearest neighbors for the KNN matrix used in training.}

\item{load_model}{character vector of 2 components (optional); paths to files created by \code{x$vae$save(file1,file2)} - model is loaded and applied instead of training a new one.}

\item{upsample}{named list \code{list(N=5000,cluster=15,method="kmeans")} or \code{NULL}; sample events by clusters (involves recomputation of the KNN matrix); if \code{NULL} nothing happens. \code{N} events per cluster, \code{cluster} number of clusters, \code{method} clustering method (either "CLARA" or "kmeans").}

\item{labels_name}{name of the label vector to use if \code{upsample$cluster} is \code{NULL} and \code{x} has multiple label vectors.}

\item{...}{unused; absorbs any extra arguments DimRed's dispatch may forward.}
}
\value{
numeric matrix of reduced coordinates.
}
\description{
Built-in "vaevictis" \code{DimRed} method. Registered automatically at package load; see
\code{\link{register_dimred_method}} for the method-plugin contract this follows.
}
